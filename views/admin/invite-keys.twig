{% extends "html.twig" %}

{% block content %}
<h2>invite keys</h2>
<hr class="mt-3 mb-2">
<div class="d-flex flex-column gap-3">
  {% for t in (0..5) %}
  <div>dasa</div>
  {% endfor %}
</div>
{% endblock %}